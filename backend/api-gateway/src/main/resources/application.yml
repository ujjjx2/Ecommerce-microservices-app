server:
  port: ${PORT:8080}

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - PATCH
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: false
      routes:
        - id: product-service
          uri: https://product-service-cvrs.onrender.com
          predicates:
            - Path=/api/products/**
            
        - id: order-service
          uri: https://order-service-okjq.onrender.com
          predicates:
            - Path=/api/orders/**
            
        - id: user-service
          uri: https://user-service-1mgh.onrender.com
          predicates:
            - Path=/api/users/**
